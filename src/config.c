#include "config.h"

/* Not sure if this is the "right way" to do this
 * But it works and I guess it looks alright for now
 */

CONFIG DEFAULT_CONFIG = {
/* Window border settings */	 
.border_normal_color = 0x222222,
.border_focused_color = 0xaaaaaa,
	.border_width = 3
};
